# Riot Database Maker

## Virtual Environment

Activate the local `uv` virtual environment:

```bash
source riot_database_3_14_2/bin/activate
```

Check the Python version:

```bash
python --version
```
